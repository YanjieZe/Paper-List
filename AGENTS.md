# Paper-List maintenance

- Keep `README.md` as the chronological inbox and `topics/*.md` as curated topic views.
- When asked to organize or classify newly added papers, use `.agents/skills/classify-paper-list/SKILL.md`.
- Do not add a web app, database, scheduled process, or generated long-form summary unless explicitly requested.
- Preserve the user's original paper lines and existing topic structure outside the requested classification batch.
