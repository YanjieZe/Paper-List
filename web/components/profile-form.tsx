"use client";
import { FormEvent, useState } from "react";
export function ProfileForm({initial}:{initial:string}){const [saved,setSaved]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch("/api/settings/profile",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({markdown:form.get("markdown")})});setSaved(response.ok)}return <form className="card card-pad stack" onSubmit={submit}><div className="field"><label>Research profile · Markdown</label><textarea className="textarea" name="markdown" defaultValue={initial} placeholder="Current projects, strong background, concepts to explain from first principles, research preferences…"/></div><div className="row"><span className="meta">Used for ranking and explanation; only reviewed promoted text enters Git.</span><button className="button green">{saved?"Saved":"Save profile"}</button></div></form>}
